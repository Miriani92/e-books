import { createSlice } from "@reduxjs/toolkit";

const booksCategorySlice = createSlice({
  name: "booksCategory",
  initialState: {},
  reducers: {},
  extraReducers: {},
});

export default booksCategorySlice.reducer;
